#include <bits/stdc++.h>

#define _ std::ios_base::sync_with_stdio(0); std::cin.tie(0);

#define f first
#define s second

typedef long long ll;

int main() { _
	int t; std::cin >> t;
	
	ll n, k, aux;
	std::map<std::string, int> map(); // min group, quant
	bool can;

	while(t--) {
		can = true;
		std::cin >> n >> k;

		for (int i = 0; i < n; i++) {
			std::cin >> aux;
			aux = min()	
		}  
		
	}

	return 0;
}
