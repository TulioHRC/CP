#include <bits/stdc++.h>

#define _ std::ios_base::sync_with_stdio(0); std::cin.tie(0);

#define f first
#define s second

typedef long long ll;

int main() { _
	int t; std::cin >> t;

	std::vector<ll> a(3e5);
	while(t--) {
		int n, k; std::cin >> n >> k;

		ll aux;
		for (int i = 0; i < n; i++) {
			std::cin >> a[i];
		}
	}

	return 0;
}
